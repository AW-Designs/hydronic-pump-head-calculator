node_modules
dist
dist-portable
*.local
.claude/settings.local.json
.DS_Store
node_modules/.tmp
*.tsbuildinfo

# Tauri build artifacts + update signing key (NEVER commit the private key)
src-tauri/target
src-tauri/updater.key
src-tauri/gen
